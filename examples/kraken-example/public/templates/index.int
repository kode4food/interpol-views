from layouts import master
import locales as str

# Context Variables
#  * lang: The locale identifier (ex: US_en)
#  * name: The name passed from the model (ex: 'kraken-interpol')

master(title, body)

def title
  str[lang].title()
end

def body
  <h1>str[lang].greeting(name)</h1>
end
